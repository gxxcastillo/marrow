{
  "name": "marrow",
  "version": "0.1.0",
  "description": "Versions each project's working memory in a private git vault, one branch per project.",
  "private": true,
  "type": "module",
  "bin": {
    "marrow": "./bin/marrow"
  },
  "scripts": {
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
